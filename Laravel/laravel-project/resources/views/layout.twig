<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Laravel example{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light mb-2">
  <div class="container">
    <a class="navbar-brand" href="/">Home</a>
    <div class="navbar-nav">
      <a class="nav-link" href="/books">Books</a>
      {% if is_admin %}
        <a class="nav-link" href="/users">Users</a>
        <a class="nav-link" href="/reserved">Reserved</a>
        <a class="nav-link" href="/db-introspect">Data base</a>
        <a class="nav-link" href="/logs">Logs</a>
      {% endif %}
    </div>
    <div class="navbar-nav ms-auto">
      {% if current_user %}
        <a class="nav-link" href="/my/reserved">My books</a>
        <form method="POST" action="{{ route('logout') }}" class="d-inline">
          {{ csrf_field()|raw }}
          <button type="submit" class="btn btn-link nav-link" style="display:inline; padding: .5rem 1rem;">Logout</button>
        </form>
      {% else %}
        <a class="nav-link" href="/login">Login</a>
        <a class="nav-link" href="/register">Register</a>
      {% endif %}
    </div>
  </div>
</nav>
<main class="container">
    {% block content %}{% endblock %}
</main>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
